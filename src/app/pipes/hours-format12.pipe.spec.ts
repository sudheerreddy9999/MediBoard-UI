import { HoursFormat12Pipe } from './hours-format12.pipe';

describe('HoursFormat12Pipe', () => {
  it('create an instance', () => {
    const pipe = new HoursFormat12Pipe();
    expect(pipe).toBeTruthy();
  });
});
